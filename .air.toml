root = "."
tmp_dir = "tmp"
[build]
  cmd = "go build -o ./tmp/bitengined ./cmd/bitengined"
  bin = "tmp/bitengined"
  delay = 1000
  include_ext = ["go"]
  exclude_dir = ["tmp", "web", "docs", "tasks", "templates"]
[misc]
  clean_on_exit = true
